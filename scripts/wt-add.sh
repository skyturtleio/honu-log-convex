#!/usr/bin/env bash
# This script has moved to ~/.local/bin/wt-add (system-wide).
# Use: wt-add <branch-name> [base-branch]
# Or via package.json: bun run wt:add <branch-name> [base-branch]
exec wt-add "$@"

2026-03-19: Wire up auth end-to-end — Fixed trailing slash in LOGTO_ENDPOINT (.env.local). Created callback route (redirects to /app/flights after login). Created convex/model/auth.ts (getAuthUser, getUserId helpers). Created convex/debug.ts (whoami query for verifying JWT auth). Created app/ route directory with auth gate in +layout.ts load function (ssr=false, localStorage cached user, redirect if unauthenticated). Created /app/flights page showing server user info + Convex identity via debug.whoami. Wired root layout with setupConvex + client.setAuth(fetchToken) using /api/convex-token endpoint. Updated landing page with Sign In/Out and "Go to Logbook" link. Added resources config to hooks.server.ts so Logto grants access tokens for the API resource. Fixed Docker networking: Convex backend in Docker can't reach localhost — added LOGTO_JWKS_URL env var using host.docker.internal. Updated IMPLEMENTATION_GUIDE_CONVEX.md with Docker networking docs, env var reference table, corrected code examples, and local vs production env var summary. Deviations: app/ directory instead of (app)/ route group (SvelteKit strips parenthesized names from URLs). Used client.setAuth() separately from setupConvex() since convex-svelte doesn't accept fetchAccessToken in options. Auth gate uses +layout.ts load function instead of $effect (idiomatic Svelte 5). Uses $app/state instead of deprecated $app/stores. Uses resolve() from $app/paths for href links.
